@echo off
color 0a
echo Please install latest python if not already installed from https://www.python.org/downloads/ amd make sure you check the checkbox that says "Add to PATH"
pip install bs4
pip install requests
pip install selenium
pip install lxml
pause